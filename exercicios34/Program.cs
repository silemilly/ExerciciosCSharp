// See https://aka.ms/new-console-template for more information
string nome;
float vendas; 

Console.WriteLine("Digite o nome: ");
nome = Console.ReadLine());
Console.WriteLine(" Digite o valor das vendas: ");
vendas = Console.WriteLine());


if (vendas > 50.000)
{
    Console.WriteLine("A comissao sera de 12%! = R$" + vendas * 0.12);
}
else (vendas >= 50.000) 
{
    {
        if (vendas > 30.000 && vendas <= 50.000) 
    }

    Console.WriteLine("A comissao sera de 9.5%! = R$" + vendas * 0.095);
}
else
{
    Console.WriteLine("A comissao sera de 7%! = R$" + vendas * 0.7);
} 